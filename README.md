Key Features: Algorithm that retrieves people aged 50+ years

This C# program queries an API and saves the results in a variable. Using a LINK query,
it then performs a SELECT query to filter the data (those older than 50).

Technologies Used: C#, .NET, Newtonsoft.Json, LINQ
